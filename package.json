{
  "name": "my-node-app",
  "version": "1.0.0",
  "description": "Node.js application with Okta authentication deployed to Azure",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "@okta/oidc-middleware": "^5.0.0",
    "dotenv": "^16.0.3"
  },
  "engines": {
    "node": ">=14.0.0"
  }
} 